export type ParamsInitFieldType = {
  fieldAnchor: string;
  label: string;
  disabled?: boolean;
  required?: boolean;
  needsPrefix?: boolean;
  singleValue?: string;
  placeholder?: string;
  helperText?: string;
};
