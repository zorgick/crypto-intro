export const BLOCK_SEARCH_FIELD = {
  fieldAnchor: 'blockSearch',
  label: 'Block number',
  helperText: 'Type -1 to get the latest block',
  placeholder: '-1',
} as const;

export const UTC_TIME_FORMAT = 'HH:mm:ss (xxx)' as const;
